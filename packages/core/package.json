{
  "name": "@hena-dev/core",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "exports": {
    ".": {
      "types": "./dist/core.d.ts",
      "default": "./dist/core.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsgo -p tsconfig.build.json && bun build ./src/core.ts --outdir ./dist --target bun --format esm --packages external --sourcemap=external",
    "clean": "rm -rf dist coverage",
    "test": "bun --bun vitest run",
    "test:coverage": "bun --bun vitest run --coverage",
    "typecheck": "tsgo --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "effect": "4.0.0-beta.83"
  },
  "devDependencies": {
    "vitest": "4.1.8"
  }
}
