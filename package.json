{
  "name": "hena",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "packageManager": "bun@1.3.14",
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "vitest": "4.1.9"
    }
  },
  "scripts": {
    "build": "turbo run build",
    "check": "biome check . && oxlint packages test/e2e vitest.config.ts vitest.shared.ts --type-aware && bun run typecheck && bun run test && knip",
    "ci": "biome ci . && oxlint packages test/e2e vitest.config.ts vitest.shared.ts --type-aware --deny-warnings && bun run typecheck && bun run build && bun run test:coverage && knip",
    "clean": "turbo run clean",
    "coverage": "turbo run test:coverage",
    "format": "biome format --write .",
    "knip": "knip",
    "lint": "biome check . && oxlint packages test/e2e vitest.config.ts vitest.shared.ts --type-aware",
    "lint:fix": "biome check --write . && oxlint packages test/e2e vitest.config.ts vitest.shared.ts --type-aware --fix",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "typecheck": "tsgo --noEmit -p tsconfig.root.json && turbo run typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.1",
    "@types/bun": "1.3.14",
    "@typescript/native-preview": "7.0.0-dev.20260624.1",
    "@vitest/coverage-istanbul": "4.1.9",
    "knip": "6.21.0",
    "oxlint": "1.71.0",
    "oxlint-tsgolint": "0.23.0",
    "turbo": "2.10.0",
    "vitest": "catalog:"
  }
}
