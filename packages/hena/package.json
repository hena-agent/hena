{
  "name": "@hena-dev/hena",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "hena": "./dist/hena.js"
  },
  "exports": {
    ".": {
      "types": "./dist/hena.d.ts",
      "default": "./dist/hena.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsgo -p tsconfig.build.json && bun build ./src/hena.ts --outdir ./dist --target bun --format esm --packages external --sourcemap=external",
    "clean": "rm -rf dist coverage",
    "test": "bun --bun vitest run",
    "test:coverage": "bun --bun vitest run --coverage",
    "typecheck": "tsgo --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "vitest": "4.1.8"
  }
}
